{
  "openapi": "3.0.0",
  "info": {
    "title": "Node.js Protobuf API",
    "description": "사용자 정보를 Protobuf 형식으로 반환하는 간단한 REST API 명세입니다. 요청은 JSON으로 보내고 응답은 Protobuf로 받습니다.",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "http://localhost:3000",
      "description": "개발 서버"
    }
  ],
  "paths": {
    "/users/{id}": {
      "get": {
        "summary": "ID로 사용자 정보 조회",
        "description": "특정 ID를 가진 사용자의 정보를 Protobuf 형식으로 직렬화하여 반환합니다.",
        "tags": ["Users"],
        "res_message" : "User",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "조회할 사용자의 ID",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "성공적인 응답. 응답 본문은 Protobuf 바이너리 데이터입니다.",
            "content": {
              "application/protobuf": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "404": {
            "description": "사용자를 찾을 수 없음"
          }
        }
      }
    },
    "/users": {
      "post": {
        "summary": "새로운 사용자 생성",
        "description": "JSON 형식으로 사용자 정보를 받아 새 사용자를 생성하고, 생성된 사용자 정보를 Protobuf 형식으로 반환합니다.",
        "tags": ["Users"],
        "req_message" : "User",
        "res_message" : "User",
        "requestBody": {
          "description": "생성할 사용자의 이름과 이메일",
          "required": true,
          "content": {
            "application/protobuf": {
              "schema": {
                "$ref": "#/components/schemas/NewUserInput"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "사용자 생성 성공. 생성된 사용자 객체를 Protobuf 형식으로 반환합니다.",
            "content": {
              "application/protobuf": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "400": {
            "description": "잘못된 요청 (이름 또는 이메일 누락)"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "User": {
        "type": "object",
        "description": "사용자 정보를 나타내는 Protobuf 메시지 스키마",
        "properties": {
          "id": {
            "type": "integer",
            "format": "int32",
            "description": "사용자 고유 ID"
          },
          "name": {
            "type": "string",
            "description": "사용자 이름"
          },
          "email": {
            "type": "string",
            "format": "email",
            "description": "사용자 이메일 주소"
          }
        }
      },
      "NewUserInput": {
        "type": "object",
        "description": "사용자 생성을 위한 JSON 입력 스키마",
        "properties": {
          "name": {
            "type": "string",
            "description": "사용자 이름",
            "example": "Charlie"
          },
          "email": {
            "type": "string",
            "format": "email",
            "description": "사용자 이메일 주소",
            "example": "charlie@example.com"
          }
        },
        "required": ["name", "email"]
      }
    }
  },
  "tags": [
    {
      "name": "Users",
      "description": "사용자 관련 API"
    }
  ]
}